"use client"
// Intercom is intentionally disabled for the self-hosted IDE — it adds a
// floating "G" launcher that the user does not want. Re-enable by restoring
// the previous content if/when needed.
export function IntercomWidget() {
  return null
}
