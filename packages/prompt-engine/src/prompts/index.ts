// Switched from "./system" (RN/Expo) to "./system-flutter" (Flutter).
// system.ts is kept on disk as legacy reference but no longer exported.
export { prompt, createSystemPrompt } from "./system-flutter";
export { convexGuidelines } from "./convex";
export * from "./sections";
